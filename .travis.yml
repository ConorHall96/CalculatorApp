#TravisCI Details
language: android
sudo: false
jdk: oraclejdk8

before_cache:

 - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock

 - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:

 directories:

 - $HOME/.gradle/caches/

 - $HOME/.gradle/wrapper/


android:
  components:
  # Uncomment the lines below if you want to
      # use the latest revision of Android SDK Tools
     - tools
     - tools
     - build-tools-27.0.3

     - android-27

     - android-$EMULATOR_API_LEVEL

     - extra-google-m2repository
     - extra-android-support
     - extra-android-m2repository # for design library

     - extra-google-google_play_services

     - sys-img-armeabi-v7a-android-27


  licenses:

     - android-sdk-preview-license-.+

     - android-sdk-license-.+

     - google-gdk-license-.+

before_install:
  - mkdir "$ANDROID_HOME/licenses" || true
  - chmod +x gradlew
  - ./gradlew dependencies || true

before_script:
# - android list sdk --extended --no-ui --all
 #- echo no | android create avd --force --name test --target android-27 --abi armeabi-v7a
# - echo "n" | android create avd --name myandroid25 -t "android-25"
 - android list targets
 #- echo no | android create avd --force -n test -t android-25
# - emulator -avd test -no-skin -no-audio -no-window &
 #- android-wait-for-emulator
# - adb shell input keyevent 82 &
script: "./gradlew assembleDebug && ./gradlew lintDebug &&./gradlew testDebug"

after_success:
- ./gradlew jacocoTestReport coveralls

addons:
  sonarcloud:
    organization: "conorhall96-github" # the key of the org you chose at step #3
    token:
      secure: b61bed167d6cbe48e8651887ca4a44d80360a6e6 # encrypted value of your token
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner

  deploy:
    provider: testfairy
    api-key: b544a5f8f077449e2592ff54a1ec56141572e066
    app-file: C:\Users\Conor\Desktop\Year4\DevOps\CalculatorApp\app\release\app-release.apk
